json.extract! program, :id, :slug, :title, :short_description, :description, :location, :image, :starts_at, :ends_at, :published, :active, :created_at, :updated_at
json.url program_url(program, format: :json)
